{% layout "server/layouts/base.liquid" %}

{% block content %}

<h1>Framer Framed Archief</h1>

{% for event in allEvents %}
  <article>
    <h2>{{ event.node.title_en }}</h2>
    <h3>Type:</h3><p>{{ event.node.type_nl }}</p>
    <h3>Startdatum:</h3><p>{{ event.node.date_start }}</p>
    <h3>Einddatum:</h3><p>{{ event.node.date_end }}</p>
    <h3>Beschrijving:</h3><div>{{ event.node.cleaned_content | raw }}</div>
    {% if event.node.extracted_images.size > 0 %}
      <div class="images">
        {% for src in event.node.extracted_images %}
          <img src="{{ src }}" alt="Afbeelding bij event">
        {% endfor %}
      </div>
    {% endif %}
    
    {% if event.rels %}
      <h3>Deelnemers:</h3>
      <ul>
        {% for rel in event.rels %}
          <li>{{ rel.name }}</li>
        {% endfor %}
      </ul>
    {% endif %}
  </article>
  <hr />
{% endfor %}

{% endblock %}
