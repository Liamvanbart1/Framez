{% layout "server/layouts/base.liquid" %}

{% block content %}

<h1 class="h1">{{ title }}</h1>
<ul class="grid">
  {% for item in years %}
    <a href="/year/{{item.year}}">{{item.year}}</a>
  {% endfor %} 
</ul>
{% endblock %}
