{% layout "server/layouts/base.liquid" %}

{% block content %}

<h1>{{ title }}</h1>
<ul>
    {% for item in organisations %}
    <li>
        <a href="/organisation/{{ item.organisation.uuid }}">
            {{ item.organisation.name }}
        </a>
    </li>
    {% endfor %}
</ul>

{% endblock %}